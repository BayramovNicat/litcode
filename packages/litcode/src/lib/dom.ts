import * as template from './template';
import * as domApi from './dom-api';

export const html = template.html;
export const repeat = template.repeat;
export const render = domApi.render;
export const mount = domApi.mount;
export const toFragment = domApi.toFragment;
